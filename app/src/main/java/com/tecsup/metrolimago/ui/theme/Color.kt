package com.tecsup.metrolimago.ui.theme

import androidx.compose.ui.graphics.Color

// --- 1. Paleta de Colores para el Tema (Modo Claro) ---
val Blue40 = Color(0xFF38608F)      // Primario (Azul oscuro)
val BlueGrey40 = Color(0xFFB0C4DE)  // Secundario (Azul claro)
val Orange40 = Color(0xFFF07A00)    // Terciario (Naranja)
val LightGrey = Color(0xFFF5F5F5)  // Fondo alternativo
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)

// --- 2. Paleta de Colores para el Tema (Modo Oscuro) ---
val Blue80 = Color(0xFFAEC6E0)      // Primario (Azul claro)
val BlueGrey80 = Color(0xFF708090)  // Secundario (Gris azulado)
val Orange80 = Color(0xFFFFB86F)    // Terciario (Naranja claro)
val DarkGrey = Color(0xFF121212)    // Fondo oscuro estándar

// --- 3. Colores Específicos de las Líneas ---
// (Estos los usa la UI. Los Hex strings los usa AppDatabase)
val Linea1Color = Color(0xFFD32F2F) // Rojo
val Linea2Color = Color(0xFFFFA000) // Naranja
val CorredorAzulColor = Color(0xFF303F9F) // Azul

